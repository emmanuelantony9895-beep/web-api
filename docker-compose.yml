# Docker Compose for Web API

version: '3.8'

services:
  api:
    build: .
    container_name: web-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
